Options to run the project:
1. Run the file "RRDTest\RRDTest\bin\Debug\RRDTest.exe".
2. Directly open the project in the latest version of Visual Studio and run it.

-The file MOCK_DATA.csv is located in the path "RRDTest\RRDTest\bin\Debug\". Also, the new converted file will be created right there.
-Currently, a converted file is already created according to the instructions. To verify that the code works, please delete the file "Converted_MOCK_DATA", located in the path already mentioned above. In case the file is not removed, the code will simply replace it.